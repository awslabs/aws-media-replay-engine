import { View } from '@aws-amplify/ui-react';
import styled from 'styled-components';

import { WHITE } from '@src/theme';

export const StyledPageLayout = styled(View)`
  color: ${WHITE};
  overflow-x: hidden;
`;
